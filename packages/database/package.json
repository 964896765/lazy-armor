{
  "name": "@lazy-armor/database",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "migrate": "tsx src/migrate.ts",
    "prepare-ci-db": "tsx src/prepare-ci-database.ts"
  },
  "dependencies": {
    "drizzle-orm": "0.45.2",
    "mysql2": "3.24.2",
    "uuid": "11.1.1"
  },
  "devDependencies": {
    "tsx": "4.20.5",
    "vitest": "4.1.11"
  }
}
