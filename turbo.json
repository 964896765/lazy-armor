{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "DATABASE_URL",
    "TEST_DATABASE_URL",
    "TEST_DATABASE_NAME",
    "MYSQL_ADMIN_URL",
    "REDIS_URL",
    "REDIS_KEY_PREFIX",
    "TEST_REDIS_KEY_PREFIX",
    "REQUIRE_EXACT_TEST_TARGET",
    "JWT_SECRET",
    "CREDENTIAL_MASTER_KEY",
    "APP_ENV",
    "NODE_ENV",
    "APP_ROLE",
    "EXPO_PUBLIC_APP_ENV",
    "RUN_REAL_DB_INTEGRATION"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    },
    "test": {
      "dependsOn": ["build", "^build"]
    }
  }
}
